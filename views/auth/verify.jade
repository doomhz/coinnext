extends ../layout

block content

  .main-content.auth(role='main')
    .wrapper
      .auth-box
        h1 Account Verification
        - if (verified)
          p.msg 
            | Your account was successfully verified.
            br
            |  <a href="/">Return to home page.</a>

        - else
          p.msg Your account could not be verified.
          